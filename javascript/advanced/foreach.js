//  for each loop
// runs only on array , then only use for each loop
var a = [1,2,3,4,5,6,7]

a.forEach(function(val){
    console.log(val*2);
})

// for each by default array par direct change nhi karta , hence the original array is preserved